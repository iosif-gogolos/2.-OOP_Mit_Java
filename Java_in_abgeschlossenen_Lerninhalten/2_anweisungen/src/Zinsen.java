public class Zinsen {
    public static void main(String[] args) {

        // --- Eingabe ---


        // --- Berechnungen ---


        // --- Ausgabe ---
        //* Docstring */
        /*
        Dreicek
        // --------------- Eingabe ---------------
        double a = 4.40;
        double b = 5.73;
        double c = 3.23;

        // --------------- Berechnung ---------------
        double alpha = Math.acos((a * a - b * b - c * c) / (-2 * b * c)) * 180 / Math.PI;
        double beta = Math.acos((b * b - a * a - c * c) / (-2 * a * c)) * 180 / Math.PI;
        double gamma = Math.acos((c * c - a * a - b * b) / (-2 * a * b)) * 180 / Math.PI;

        // --------------- Ausgabe ---------------
        System.out.println("Winkel alpha = " + alpha + " Grad");
        System.out.println("Winkel beta = " + beta + " Grad");
        System.out.println("Winkel gamma = " + gamma + " Grad");
        */

    }
}
